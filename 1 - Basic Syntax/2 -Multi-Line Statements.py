#Define each item
item_one = 4
item_two = 6
item_three = 8
#Define total of each item
#Use the line continuation character (\) to denote that the line should continue.
total = item_one + \
    item_two + \
    item_three
#Print total
print(total)