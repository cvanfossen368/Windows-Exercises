#Assume variable a holds the value 10 and variable b holds..
#..the value 20, then -
a = 10
b = 20

# = : Assigns value from right side operands to left side operand.
#c = a + b assigns value of a + b into c
c = a + b
print(c)

# += Add AND : Adds right operand to the left operand and assigns the result.
#..to left operand.
#c += a is equivalent to c = c + a
c = 30
c += a
print(c)

# -= Subtract AND : Subtracts right operand from the left operand..
#.. and assigns the result to the left operand.
#c -= a is equivalent to c = c - a
c = 40
c -= a
print(c)

# *= Multiple AND : Multiplies right operand with the left operand..
#.. and assigns the result to left operand.
#c *= a is equivalent to c = c * a
c = 30
c *= a
print(c)

# /= Divide AND : Divides left operand with the right operand  and..
#.. assigns the result to left operand.
#c /= a is equivalent to c = c / ac /= a is equivalent to c = c / a
c = 300
c /= a
print(c)

# %= Modulus AND : Takes modulus using two operands and assigns the..
#.. result to the left operand.
# c %= a is equivalent to c = c % a
c = 30
c %= a
print(c)

# **= Exponent AND : Performs exponential (power) calculation on operators..
#..and assigns value to the left operand.
#c **= a is equivalent to c = c ** a
c = 5
c **= a
print(c)

# //= Floor Division : Performs floor division on operators and assign..
#..value to the left operand.
#c //= a is equivalent to c = c // a
c = 9765625
c //= a
print(c)


